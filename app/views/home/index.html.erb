<h3>Campeonatos</h3>
<div class="well sidebar-nav">
  <ul class="nav nav-list">
    <% @championships.each do |c| %>
       <li><%= link_to c.name, dashboard_path(c)  %></li>
    <% end %>
  </ul>
</div>